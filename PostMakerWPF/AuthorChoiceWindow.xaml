<Window x:Class="PostMakerWPF.AuthorChoiceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PostMakerWPF"
        mc:Ignorable="d"
        Title="AuthorChoiceWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="7*"></RowDefinition>

        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Margin="0, 0, 0, 8" FontSize="28" TextAlignment="Center" Padding="0, 7, 0,0" 
                   FontFamily="Bahnschrift Light"  Background="#FFC0DC94">--Виберіть автора--</TextBlock>

        <DataGrid Name="namesDataGridView"   Grid.Row="1" Margin="5, 8, 5, 0" MouseDoubleClick="namesDataGridView_MouseDoubleClick">
            <DataGrid.Columns>
                 
            </DataGrid.Columns>
        </DataGrid>
        <!--<ListView Name="namesListView" Grid.Row="1" Margin="5, 8, 5, 0">
        </ListView>-->
    </Grid>
</Window>
